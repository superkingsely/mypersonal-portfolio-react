import React from 'react'
import Newsletter from '../Newsletter/Newsletter'

const Main = ({children}:any) => {
  return (
    <main>
        {children}
        {/* <Newsletter/> */}
    </main>
  )
}

export default Main